{%extends "AppBundle:admin:indexAdmin.html.twig"%}

{% block container -%}
<div class="descriptionArea">
<h1>Room</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ entity.number }}</td>
            </tr>
            <tr>
                <th>Price Per Night</th>
                <td>{{ entity.pricePerNight }}</td>
            </tr>
            <tr>
                <th>Has Wireless</th>
                <td>{{ entity.hasWireless }}</td>
            </tr>
            <tr>
                <th>Has Air Conditioning</th>
                <td>{{ entity.hasAirConditioning }}</td>
            </tr>
            <tr>
                <th>Has TV</th>
                <td>{{ entity.hasTv }}</td>
            </tr>
            <tr>
                <th>Has Minibar</th>
                <td>{{ entity.hasMinibar }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('room') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('room_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
</div>
{% endblock %}
