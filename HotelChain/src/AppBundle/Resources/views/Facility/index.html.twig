{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Facility list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idfacility</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('facility_show', { 'id': entity.idfacility }) }}">{{ entity.idfacility }}</a></td>
                <td>{{ entity.name }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('facility_show', { 'id': entity.idfacility }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('facility_edit', { 'id': entity.idfacility }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('facility_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
