{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Rating</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Idrating</th>
                <td>{{ entity.idrating }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ entity.value }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('rating') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('rating_edit', { 'id': entity.idrating }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
