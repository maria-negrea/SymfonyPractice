{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Roomtype list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idroomtype</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('roomtype_show', { 'id': entity.idroomtype }) }}">{{ entity.idroomtype }}</a></td>
                <td>{{ entity.name }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('roomtype_show', { 'id': entity.idroomtype }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('roomtype_edit', { 'id': entity.idroomtype }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('roomtype_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
